# MCP Python SDK 深度高级使用场景

> 本文档面向需要深度定制 MCP 服务的高级开发者，涵盖底层 API、OAuth 认证、Task 长任务、ASGI 挂载、SSE 断线重连、自定义传输等高级场景。

---

## 目录

- [一、场景 1：低层 Server API —— 完全控制请求处理](#一场景-1低层-server-api--完全控制请求处理)
- [二、场景 2：OAuth 2.0 认证 —— 保护 MCP 服务](#二场景-2oauth-20-认证--保护-mcp-服务)
- [三、场景 3：OAuth 客户端 —— 浏览器授权流程](#三场景-3oauth-客户端--浏览器授权流程)
- [四、场景 4：Task（长任务）—— 异步任务调度](#四场景-4task长任务--异步任务调度)
- [五、场景 5：交互式 Task —— 任务中进行 Elicitation 和 Sampling](#五场景-5交互式-task--任务中进行-elicitation-和-sampling)
- [六、场景 6：ASGI 挂载 —— 集成到 Starlette/FastAPI](#六场景-6asgi-挂载--集成到-starlettefastapi)
- [七、场景 7：多服务器挂载 —— 单一端口暴露多个 MCP 服务](#七场景-7多服务器挂载--单一端口暴露多个-mcp-服务)
- [八、场景 8：SSE 断线重连与 EventStore](#八场景-8sse-断线重连与-eventstore)
- [九、场景 9：分页处理 —— 大数据集的资源列表](#九场景-9分页处理--大数据集的资源列表)
- [十、场景 10：自定义路由 —— 在 MCP 服务器中添加 HTTP 端点](#十场景-10自定义路由--在-mcp-服务器中添加-http-端点)
- [十一、场景 11：URL Elicitation —— OAuth 授权链路](#十一场景-11url-elicitation--oauth-授权链路)
- [十二、场景 12：结构化输出 + 低层 Server 的 output_schema](#十二场景-12结构化输出--低层-server-的-output_schema)
- [十三、场景 13：CallToolResult 直接返回 + _meta 元数据](#十三场景-13calltoolresult-直接返回--_meta-元数据)
- [十四、场景 14：CORS 配置 —— 浏览器端 MCP 客户端](#十四场景-14cors-配置--浏览器端-mcp-客户端)
- [十五、场景 15：安全传输 —— DNS 重绑定防护](#十五场景-15安全传输--dns-重绑定防护)
- [十六、架构模式总结](#十六架构模式总结)

---

## 一、场景 1：低层 Server API —— 完全控制请求处理

**场景描述**：高层 `MCPServer` 适合快速开发，但当需要完全控制请求处理流程（自定义分页、动态 schema、中间件逻辑）时，需要使用低层 `Server` API。

### 代码

```python
import asyncio
import mcp.server.stdio
from mcp import types
from mcp.server import Server
from mcp.server.context import ServerRequestContext

# 创建低层 Server
server = Server("advanced-server")

# 手动定义工具列表
async def handle_list_tools(
    ctx: ServerRequestContext,
    params: types.PaginatedRequestParams | None,
) -> types.ListToolsResult:
    return types.ListToolsResult(
        tools=[
            types.Tool(
                name="analyze",
                description="Analyze data with custom schema",
                input_schema={
                    "type": "object",
                    "properties": {
                        "data": {"type": "array", "items": {"type": "number"}},
                        "method": {
                            "type": "string",
                            "enum": ["mean", "median", "std"],
                        },
                    },
                    "required": ["data", "method"],
                },
                # 手动定义输出 schema
                output_schema={
                    "type": "object",
                    "properties": {
                        "result": {"type": "number"},
                        "method": {"type": "string"},
                        "count": {"type": "integer"},
                    },
                    "required": ["result", "method", "count"],
                },
            )
        ]
    )

# 手动处理工具调用
async def handle_call_tool(
    ctx: ServerRequestContext,
    params: types.CallToolRequestParams,
) -> types.CallToolResult:
    if params.name == "analyze":
        data = params.arguments["data"]
        method = params.arguments["method"]

        import statistics
        if method == "mean":
            result = statistics.mean(data)
        elif method == "median":
            result = statistics.median(data)
        elif method == "std":
            result = statistics.stdev(data) if len(data) > 1 else 0
        else:
            return types.CallToolResult(
                content=[types.TextContent(type="text", text=f"Unknown method: {method}")],
                is_error=True,
            )

        structured = {"result": result, "method": method, "count": len(data)}
        return types.CallToolResult(
            content=[types.TextContent(type="text", text=f"{method}={result}")],
            structured_content=structured,
        )

    return types.CallToolResult(
        content=[types.TextContent(type="text", text=f"Unknown tool: {params.name}")],
        is_error=True,
    )

# 注册处理器
server = Server(
    "advanced-server",
    on_list_tools=handle_list_tools,
    on_call_tool=handle_call_tool,
)

async def run():
    async with mcp.server.stdio.stdio_server() as (read_stream, write_stream):
        await server.run(
            read_stream,
            write_stream,
            server.create_initialization_options(),
        )

if __name__ == "__main__":
    asyncio.run(run())
```

### 交互示例

```
await session.call_tool("analyze", {"data": [1, 2, 3, 4, 5], "method": "mean"})
# content: [TextContent(text="mean=3")]
# structured_content: {"result": 3, "method": "mean", "count": 5}
```

**要点**：
- 低层 API 需要手动编写 `input_schema` 和 `output_schema`（JSON Schema 格式）
- 通过回调函数方式注册处理器
- 完全控制响应格式和错误处理

---

## 二、场景 2：OAuth 2.0 认证 —— 保护 MCP 服务

**场景描述**：MCP 服务器需要 OAuth 2.0 Bearer Token 验证，只有通过认证的客户端才能调用工具。

### 资源服务器代码

```python
from mcp.server.mcpserver import MCPServer
from mcp.server.auth.provider import AccessToken, TokenVerifier
from mcp.server.auth.settings import AuthSettings
from pydantic import AnyHttpUrl

class IntrospectionTokenVerifier(TokenVerifier):
    """通过令牌内省端点验证 token"""

    def __init__(self, introspection_url: str, client_id: str, client_secret: str):
        self.introspection_url = introspection_url
        self.client_id = client_id
        self.client_secret = client_secret

    async def verify_token(self, token: str) -> AccessToken | None:
        import httpx

        async with httpx.AsyncClient() as client:
            response = await client.post(
                self.introspection_url,
                data={"token": token},
                auth=(self.client_id, self.client_secret),
            )
            data = response.json()

            if not data.get("active"):
                return None

            return AccessToken(
                token=token,
                client_id=data.get("client_id", ""),
                scopes=data.get("scope", "").split(),
                expires_at=data.get("exp"),
            )

# 创建受保护的 MCP 服务器
mcp = MCPServer(
    "Protected API",
    token_verifier=IntrospectionTokenVerifier(
        introspection_url="https://auth.example.com/oauth/introspect",
        client_id="mcp-resource-server",
        client_secret="secret",
    ),
    auth=AuthSettings(
        issuer_url=AnyHttpUrl("https://auth.example.com"),
        resource_server_url=AnyHttpUrl("http://localhost:8000"),
        required_scopes=["mcp:tools"],
    ),
)

@mcp.tool()
def get_secret_data(key: str) -> str:
    """Get secret data (requires authentication)"""
    secrets = {"api_key": "sk-xxxx", "db_password": "pg-yyyy"}
    return secrets.get(key, "not found")

if __name__ == "__main__":
    mcp.run(transport="streamable-http", host="0.0.0.0", port=8000)
```

### 交互流程

```
# 未认证请求
POST http://localhost:8000/mcp
→ 401 Unauthorized
← WWW-Authenticate: Bearer resource_metadata="http://localhost:8000/.well-known/oauth-protected-resource"

# 客户端发现认证要求 → 获取 Protected Resource Metadata
GET http://localhost:8000/.well-known/oauth-protected-resource
← {"resource": "http://localhost:8000", "authorization_servers": ["https://auth.example.com"], ...}

# 客户端走 OAuth 流程获取 token
# ... (authorization code flow with PKCE) ...

# 已认证请求
POST http://localhost:8000/mcp
Authorization: Bearer eyJhbGciOi...
→ 200 OK (正常 MCP 通信)
```

---

## 三、场景 3：OAuth 客户端 —— 浏览器授权流程

**场景描述**：客户端需要通过 OAuth 2.0 授权码流程获取令牌来连接受保护的 MCP 服务器。

### 代码

```python
import anyio
import webbrowser
from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client
from mcp.client.auth.oauth2 import OAuthClientProvider, TokenStorage
from mcp.shared.auth import OAuthClientMetadata, OAuthToken, OAuthClientInformationFull

class FileTokenStorage(TokenStorage):
    """将 token 持久化到文件"""

    def __init__(self, path: str = ".mcp_tokens.json"):
        self.path = path

    async def get_tokens(self) -> OAuthToken | None:
        import json
        try:
            with open(self.path) as f:
                data = json.load(f)
                return OAuthToken(**data.get("tokens", {})) if data.get("tokens") else None
        except FileNotFoundError:
            return None

    async def set_tokens(self, tokens: OAuthToken) -> None:
        import json
        data = {}
        try:
            with open(self.path) as f:
                data = json.load(f)
        except FileNotFoundError:
            pass
        data["tokens"] = tokens.model_dump()
        with open(self.path, "w") as f:
            json.dump(data, f)

    async def get_client_info(self) -> OAuthClientInformationFull | None:
        import json
        try:
            with open(self.path) as f:
                data = json.load(f)
                info = data.get("client_info")
                return OAuthClientInformationFull(**info) if info else None
        except FileNotFoundError:
            return None

    async def set_client_info(self, info: OAuthClientInformationFull) -> None:
        import json
        data = {}
        try:
            with open(self.path) as f:
                data = json.load(f)
        except FileNotFoundError:
            pass
        data["client_info"] = info.model_dump()
        with open(self.path, "w") as f:
            json.dump(data, f)

async def main():
    import httpx

    # 设置 OAuth 回调事件
    auth_code_event = anyio.Event()
    received_code = {"value": None}

    def redirect_handler(url: str) -> None:
        """浏览器打开授权 URL"""
        print(f"请在浏览器中授权: {url}")
        webbrowser.open(url)

    def callback_handler(code: str) -> None:
        """授权码回调"""
        received_code["value"] = code
        auth_code_event.set()

    # 创建 OAuth Provider
    oauth_provider = OAuthClientProvider(
        server_url="http://localhost:8000",
        client_metadata=OAuthClientMetadata(
            client_name="My MCP Client",
            redirect_uris=["http://localhost:3030/callback"],
            grant_types=["authorization_code", "refresh_token"],
            response_types=["code"],
        ),
        storage=FileTokenStorage(),
        redirect_handler=redirect_handler,
        callback_handler=callback_handler,
    )

    # 使用 OAuth 认证连接
    async with httpx.AsyncClient() as http_client:
        oauth_auth = oauth_provider.create_auth(http_client)

        async with streamable_http_client(
            "http://localhost:8000/mcp",
            httpx_client_factory=lambda: httpx.AsyncClient(auth=oauth_auth),
        ) as (read, write):
            async with ClientSession(read, write) as session:
                await session.initialize()
                print(f"已连接到: {session.initialize_result.server_info.name}")

                tools = await session.list_tools()
                for tool in tools.tools:
                    print(f"  工具: {tool.name}")

anyio.run(main)
```

### 交互流程

```
# 首次运行（无缓存 token）：
$ python oauth_client.py
请在浏览器中授权: https://auth.example.com/authorize?client_id=...&redirect_uri=...

# 浏览器打开授权页面 → 用户登录并授权 → 回调到 localhost:3030/callback
# OAuth Provider 自动：
# 1. 发现 Protected Resource Metadata (RFC 9728)
# 2. 发现 Authorization Server Metadata (RFC 8414)
# 3. 动态注册客户端 (RFC 7591) 或使用 CIMD
# 4. 生成 PKCE challenge
# 5. 重定向用户到授权页面
# 6. 接收授权码
# 7. 用授权码换取 access_token + refresh_token
# 8. 保存 token 到 FileTokenStorage

已连接到: Protected API
  工具: get_secret_data

# 后续运行（有缓存 token）：
$ python oauth_client.py
已连接到: Protected API           ← 直接连接，无需再次授权
  工具: get_secret_data
```

---

## 四、场景 4：Task（长任务）—— 异步任务调度

**场景描述**：工具执行耗时很长（几分钟到几小时），需要异步任务模式。客户端发起后立即返回任务 ID，后续轮询获取结果。

### 服务端代码

```python
import asyncio
from mcp import types
from mcp.server import Server
from mcp.server.context import ServerRequestContext

server = Server("task-server")
server.experimental.enable_tasks()

async def handle_list_tools(
    ctx: ServerRequestContext,
    params: types.PaginatedRequestParams | None,
) -> types.ListToolsResult:
    return types.ListToolsResult(
        tools=[
            types.Tool(
                name="train_model",
                description="Train a machine learning model (long-running)",
                input_schema={
                    "type": "object",
                    "properties": {
                        "dataset": {"type": "string"},
                        "epochs": {"type": "integer", "default": 10},
                    },
                    "required": ["dataset"],
                },
                # 标记为必须以 Task 模式调用
                task_support=types.TASK_REQUIRED,
            )
        ]
    )

async def handle_call_tool(
    ctx: ServerRequestContext,
    params: types.CallToolRequestParams,
) -> types.CallToolResult:
    if params.name == "train_model":
        dataset = params.arguments["dataset"]
        epochs = params.arguments.get("epochs", 10)

        # 定义后台工作函数
        async def train_work(task):
            for epoch in range(epochs):
                await task.update_status(
                    message=f"训练中: epoch {epoch + 1}/{epochs}",
                    progress=epoch + 1,
                    total=epochs,
                )
                await asyncio.sleep(2)  # 模拟训练

            return types.CallToolResult(
                content=[types.TextContent(
                    type="text",
                    text=f"模型训练完成: {dataset}, {epochs} epochs, accuracy=0.95"
                )],
                structured_content={
                    "dataset": dataset,
                    "epochs": epochs,
                    "accuracy": 0.95,
                    "status": "completed",
                },
            )

        # 启动异步任务
        return await ctx.experimental.run_task(train_work)

    return types.CallToolResult(
        content=[types.TextContent(type="text", text="Unknown tool")],
        is_error=True,
    )

server = Server(
    "task-server",
    on_list_tools=handle_list_tools,
    on_call_tool=handle_call_tool,
)
server.experimental.enable_tasks()
```

### 客户端代码

```python
import anyio
from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client
from mcp.types import CallToolResult

async def main():
    async with streamable_http_client("http://localhost:8000/mcp") as (read, write):
        async with ClientSession(read, write) as session:
            await session.initialize()

            # 以 Task 模式调用工具
            result = await session.experimental.call_tool_as_task(
                "train_model",
                {"dataset": "imagenet", "epochs": 5},
                ttl=3600,  # 任务保活 1 小时
            )

            task_id = result.task.task_id
            print(f"任务已创建: {task_id}")

            # 轮询任务状态
            async for status in session.experimental.poll_task(task_id):
                print(f"  状态: {status.status} - {status.message}")
                if status.progress and status.total:
                    pct = status.progress / status.total * 100
                    print(f"  进度: {pct:.0f}%")

                if status.status in ("completed", "failed", "canceled"):
                    break

            # 获取最终结果
            final_result = await session.experimental.get_task_result(
                task_id, CallToolResult
            )
            print(f"\n训练结果: {final_result.structured_content}")

anyio.run(main)
```

### 交互输出

```
任务已创建: task-abc123
  状态: working - 训练中: epoch 1/5
  进度: 20%
  状态: working - 训练中: epoch 2/5
  进度: 40%
  状态: working - 训练中: epoch 3/5
  进度: 60%
  状态: working - 训练中: epoch 4/5
  进度: 80%
  状态: working - 训练中: epoch 5/5
  进度: 100%
  状态: completed - None

训练结果: {'dataset': 'imagenet', 'epochs': 5, 'accuracy': 0.95, 'status': 'completed'}
```

---

## 五、场景 5：交互式 Task —— 任务中进行 Elicitation 和 Sampling

**场景描述**：长任务执行过程中需要向用户请求输入或调用 LLM。

### 服务端代码

```python
from mcp import types
from mcp.server import Server
from mcp.server.context import ServerRequestContext

server = Server("interactive-task-server")
server.experimental.enable_tasks()

async def handle_call_tool(
    ctx: ServerRequestContext,
    params: types.CallToolRequestParams,
) -> types.CallToolResult:
    if params.name == "confirm_and_execute":
        async def interactive_work(task):
            # 第一步：向用户请求确认
            elicit_result = await task.elicit(
                message="即将执行危险操作：删除所有临时文件。是否继续？",
                schema={
                    "type": "object",
                    "properties": {
                        "confirmed": {"type": "boolean", "description": "确认执行？"},
                        "backup_first": {"type": "boolean", "description": "是否先备份？", "default": True},
                    },
                    "required": ["confirmed"],
                },
            )

            if elicit_result.action != "accept" or not elicit_result.content.get("confirmed"):
                return types.CallToolResult(
                    content=[types.TextContent(type="text", text="操作已取消")]
                )

            # 第二步：使用 LLM 生成操作报告
            await task.update_status(message="正在生成操作报告...")
            sampling_result = await task.create_message(
                messages=[
                    types.SamplingMessage(
                        role="user",
                        content=types.TextContent(
                            type="text",
                            text="请生成一份简短的文件清理操作报告，包含时间、操作类型、影响范围。",
                        ),
                    )
                ],
                max_tokens=200,
            )

            report = sampling_result.content.text if sampling_result.content.type == "text" else "报告生成失败"

            return types.CallToolResult(
                content=[types.TextContent(type="text", text=f"操作完成\n\n{report}")]
            )

        return await ctx.experimental.run_task(interactive_work)

# ... 注册处理器 ...
```

### 客户端代码

```python
import anyio
from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client
from mcp.types import (
    CreateMessageRequestParams,
    CreateMessageResult,
    ElicitRequestParams,
    ElicitResult,
    TextContent,
)

# Elicitation 回调：处理服务器发来的用户确认请求
async def handle_elicitation(ctx, params: ElicitRequestParams):
    print(f"\n[服务器请求] {params.message}")
    # 实际应用中这里会弹出 UI 让用户输入
    return ElicitResult(
        action="accept",
        content={"confirmed": True, "backup_first": True},
    )

# Sampling 回调：处理服务器发来的 LLM 请求
async def handle_sampling(ctx, params: CreateMessageRequestParams):
    print(f"\n[LLM 请求] {params.messages[-1].content.text}")
    # 实际应用中这里会调用 LLM API
    return CreateMessageResult(
        role="assistant",
        content=TextContent(type="text", text="操作报告：2025-01-16 清理了 42 个临时文件"),
        model="gpt-4",
    )

async def main():
    async with streamable_http_client("http://localhost:8000/mcp") as (read, write):
        async with ClientSession(
            read,
            write,
            elicitation_callback=handle_elicitation,
            sampling_callback=handle_sampling,
        ) as session:
            await session.initialize()

            result = await session.experimental.call_tool_as_task(
                "confirm_and_execute", {}
            )

            async for status in session.experimental.poll_task(result.task.task_id):
                print(f"[任务状态] {status.status}: {status.message}")
                if status.status in ("completed", "failed"):
                    break

anyio.run(main)
```

---

## 六、场景 6：ASGI 挂载 —— 集成到 Starlette/FastAPI

**场景描述**：将 MCP 服务器作为子应用挂载到现有的 Web 框架中，与其他 HTTP 端点共存。

### Starlette 集成

```python
import contextlib
from starlette.applications import Starlette
from starlette.routing import Mount, Route
from starlette.responses import JSONResponse
from mcp.server.mcpserver import MCPServer

mcp = MCPServer("Embedded MCP")

@mcp.tool()
def hello(name: str) -> str:
    return f"Hello, {name}!"

# 普通 HTTP 端点
async def health_check(request):
    return JSONResponse({"status": "ok"})

async def api_info(request):
    return JSONResponse({
        "name": mcp.name,
        "tools": [t.name for t in await mcp.list_tools()],
    })

@contextlib.asynccontextmanager
async def lifespan(app: Starlette):
    async with mcp.session_manager.run():
        yield

app = Starlette(
    routes=[
        Route("/health", health_check),
        Route("/api/info", api_info),
        # MCP 端点挂载在 /mcp 路径下
        Mount("/mcp", app=mcp.streamable_http_app(
            stateless_http=True,
            json_response=True,
        )),
    ],
    lifespan=lifespan,
)

# 启动：uvicorn main:app --host 0.0.0.0 --port 8000
```

### FastAPI 集成

```python
import contextlib
from fastapi import FastAPI
from mcp.server.mcpserver import MCPServer

mcp = MCPServer("FastAPI MCP")

@mcp.tool()
def compute(expression: str) -> str:
    return str(eval(expression))

@contextlib.asynccontextmanager
async def lifespan(app: FastAPI):
    async with mcp.session_manager.run():
        yield

app = FastAPI(lifespan=lifespan)

# 普通 FastAPI 路由
@app.get("/api/status")
async def status():
    return {"status": "running"}

# 挂载 MCP
app.mount("/mcp", mcp.streamable_http_app(
    stateless_http=True,
    json_response=True,
))

# 启动：uvicorn main:app --host 0.0.0.0 --port 8000
# MCP 端点: http://localhost:8000/mcp
# API 端点: http://localhost:8000/api/status
```

---

## 七、场景 7：多服务器挂载 —— 单一端口暴露多个 MCP 服务

**场景描述**：一个 HTTP 服务同时承载多个独立的 MCP 服务器，根据路径路由。

### 代码

```python
import contextlib
from starlette.applications import Starlette
from starlette.routing import Mount
from mcp.server.mcpserver import MCPServer

# 数学服务器
math_mcp = MCPServer("Math Service")

@math_mcp.tool()
def add(a: float, b: float) -> float:
    return a + b

@math_mcp.tool()
def multiply(a: float, b: float) -> float:
    return a * b

# 文本服务器
text_mcp = MCPServer("Text Service")

@text_mcp.tool()
def uppercase(text: str) -> str:
    return text.upper()

@text_mcp.tool()
def word_count(text: str) -> int:
    return len(text.split())

# 数据服务器
data_mcp = MCPServer("Data Service")

@data_mcp.tool()
def format_json(data: dict) -> str:
    import json
    return json.dumps(data, indent=2, ensure_ascii=False)

@contextlib.asynccontextmanager
async def lifespan(app: Starlette):
    async with contextlib.AsyncExitStack() as stack:
        await stack.enter_async_context(math_mcp.session_manager.run())
        await stack.enter_async_context(text_mcp.session_manager.run())
        await stack.enter_async_context(data_mcp.session_manager.run())
        yield

app = Starlette(
    routes=[
        Mount("/math", app=math_mcp.streamable_http_app(
            stateless_http=True, json_response=True
        )),
        Mount("/text", app=text_mcp.streamable_http_app(
            stateless_http=True, json_response=True
        )),
        Mount("/data", app=data_mcp.streamable_http_app(
            stateless_http=True, json_response=True
        )),
    ],
    lifespan=lifespan,
)

# 客户端连接不同服务：
# Math: http://localhost:8000/math
# Text: http://localhost:8000/text
# Data: http://localhost:8000/data
```

### 客户端使用

```python
import anyio
from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client

async def main():
    # 连接数学服务
    async with streamable_http_client("http://localhost:8000/math") as (r, w):
        async with ClientSession(r, w) as session:
            await session.initialize()
            result = await session.call_tool("add", {"a": 10, "b": 20})
            print(f"10 + 20 = {result.content[0].text}")

    # 连接文本服务
    async with streamable_http_client("http://localhost:8000/text") as (r, w):
        async with ClientSession(r, w) as session:
            await session.initialize()
            result = await session.call_tool("uppercase", {"text": "hello"})
            print(f"HELLO = {result.content[0].text}")

anyio.run(main)
```

---

## 八、场景 8：SSE 断线重连与 EventStore

**场景描述**：长时间运行的工具通过 SSE 流式发送通知，支持网络断线后基于 Last-Event-ID 恢复。

### 服务端代码

```python
import anyio
from mcp import types
from mcp.server import Server
from mcp.server.context import ServerRequestContext
from mcp.server.streamable_http import (
    StreamableHTTPServerTransport,
    EventStore,
    EventId,
    EventMessage,
)

class InMemoryEventStore(EventStore):
    """内存事件存储，用于 SSE 断线恢复"""

    def __init__(self):
        self._events: dict[str, list[tuple[EventId, EventMessage]]] = {}
        self._counter = 0

    async def store_event(
        self, stream_id: str, message: types.JSONRPCMessage | None
    ) -> EventId:
        self._counter += 1
        event_id = f"evt-{self._counter}"
        if stream_id not in self._events:
            self._events[stream_id] = []
        self._events[stream_id].append((event_id, EventMessage(
            event_id=event_id,
            message=message,
        )))
        return event_id

    async def replay_events_after(
        self, stream_id: str, last_event_id: str, max_events: int = 100
    ) -> list[EventMessage]:
        events = self._events.get(stream_id, [])
        found = False
        result = []
        for eid, emsg in events:
            if found:
                result.append(emsg)
                if len(result) >= max_events:
                    break
            if eid == last_event_id:
                found = True
        return result

server = Server("resilient-server")

async def handle_call_tool(
    ctx: ServerRequestContext,
    params: types.CallToolRequestParams,
) -> types.CallToolResult:
    if params.name == "long_process":
        total = 20
        for i in range(total):
            # 发送进度通知（会被 EventStore 记录）
            await ctx.session.send_log_message(
                level="info",
                data=f"步骤 {i + 1}/{total}",
                related_request_id=ctx.request_id,
            )
            await anyio.sleep(1)

            # 在关键节点关闭 SSE 流（触发客户端重连）
            if i == 9 and ctx.close_sse_stream:
                await ctx.close_sse_stream()

        return types.CallToolResult(
            content=[types.TextContent(type="text", text=f"完成 {total} 步")]
        )

# ... 注册处理器 ...

# 创建带 EventStore 的 Starlette 应用
from starlette.applications import Starlette
from starlette.routing import Mount

event_store = InMemoryEventStore()

app = Starlette(routes=[
    Mount("/", app=server.streamable_http_app(
        event_store=event_store,
        retry_interval=100,  # 100ms 重试间隔
    )),
])
```

### 客户端交互

```
# 客户端连接并调用 long_process
# 收到步骤 1-10 的日志通知（通过 SSE 流）
# 服务器在步骤 10 时关闭 SSE 流
# 客户端自动重连，发送 Last-Event-ID
# EventStore 回放错过的事件
# 继续收到步骤 11-20 的通知
# 最终收到结果
```

---

## 九、场景 9：分页处理 —— 大数据集的资源列表

**场景描述**：当工具、资源或提示数量很大时，需要分页返回。

### 服务端（低层 API）

```python
from mcp import types
from mcp.server import Server
from mcp.server.context import ServerRequestContext

server = Server("paginated-server")

# 模拟大数据集
ALL_TOOLS = [
    types.Tool(
        name=f"tool_{i}",
        description=f"Tool number {i}",
        input_schema={"type": "object", "properties": {}},
    )
    for i in range(100)
]

PAGE_SIZE = 10

async def handle_list_tools(
    ctx: ServerRequestContext,
    params: types.PaginatedRequestParams | None,
) -> types.ListToolsResult:
    cursor = params.cursor if params else None
    start = int(cursor) if cursor else 0

    page = ALL_TOOLS[start : start + PAGE_SIZE]
    next_start = start + PAGE_SIZE
    next_cursor = str(next_start) if next_start < len(ALL_TOOLS) else None

    return types.ListToolsResult(tools=page, next_cursor=next_cursor)

server = Server("paginated-server", on_list_tools=handle_list_tools)
```

### 客户端

```python
async def list_all_tools(session):
    """分页获取所有工具"""
    all_tools = []
    cursor = None

    while True:
        result = await session.list_tools(cursor=cursor)
        all_tools.extend(result.tools)
        print(f"获取了 {len(result.tools)} 个工具 (总计: {len(all_tools)})")

        if result.next_cursor:
            cursor = result.next_cursor
        else:
            break

    return all_tools

# 使用
tools = await list_all_tools(session)
print(f"共 {len(tools)} 个工具")
```

### 交互输出

```
获取了 10 个工具 (总计: 10)
获取了 10 个工具 (总计: 20)
获取了 10 个工具 (总计: 30)
...
获取了 10 个工具 (总计: 100)
共 100 个工具
```

---

## 十、场景 10：自定义路由 —— 在 MCP 服务器中添加 HTTP 端点

**场景描述**：在 MCPServer 中添加自定义的非 MCP HTTP 路由（如 webhook、健康检查）。

### 代码

```python
from mcp.server.mcpserver import MCPServer
from starlette.requests import Request
from starlette.responses import JSONResponse

mcp = MCPServer("Extended Server")

@mcp.tool()
def process(data: str) -> str:
    return f"processed: {data}"

# 自定义 HTTP 路由
@mcp.custom_route("/webhook", methods=["POST"])
async def handle_webhook(request: Request) -> JSONResponse:
    body = await request.json()
    # 处理 webhook
    return JSONResponse({"status": "received", "data": body})

@mcp.custom_route("/health", methods=["GET"])
async def health_check(request: Request) -> JSONResponse:
    tools = await mcp.list_tools()
    return JSONResponse({
        "status": "healthy",
        "server": mcp.name,
        "tools_count": len(tools),
    })

@mcp.custom_route("/metrics", methods=["GET"])
async def metrics(request: Request) -> JSONResponse:
    return JSONResponse({
        "requests_total": 42,
        "errors_total": 0,
        "uptime_seconds": 3600,
    })
```

### 交互

```
# MCP 通信
POST http://localhost:8000/mcp → 正常 MCP JSON-RPC

# 自定义端点
GET http://localhost:8000/health
← {"status": "healthy", "server": "Extended Server", "tools_count": 1}

POST http://localhost:8000/webhook {"event": "data_updated"}
← {"status": "received", "data": {"event": "data_updated"}}

GET http://localhost:8000/metrics
← {"requests_total": 42, "errors_total": 0, "uptime_seconds": 3600}
```

---

## 十一、场景 11：URL Elicitation —— OAuth 授权链路

**场景描述**：工具需要用户在浏览器中完成某个操作（如 OAuth 授权、支付确认），通过 URL Elicitation 引导用户。

### 代码

```python
import uuid
from mcp.server.mcpserver import MCPServer, Context
from mcp.shared.exceptions import UrlElicitationRequiredError
from mcp.types import ElicitRequestURLParams

mcp = MCPServer("Integration Service")

@mcp.tool()
async def connect_github(ctx: Context) -> str:
    """Connect to GitHub account"""
    elicitation_id = str(uuid.uuid4())

    # 方式 1：抛出异常（工具尚未执行完毕时要求用户先完成 URL 交互）
    raise UrlElicitationRequiredError([
        ElicitRequestURLParams(
            mode="url",
            message="请在浏览器中授权 GitHub 访问",
            url=f"https://github.com/login/oauth/authorize?client_id=xxx&state={elicitation_id}",
            elicitation_id=elicitation_id,
        )
    ])

@mcp.tool()
async def initiate_payment(amount: float, currency: str, ctx: Context) -> str:
    """Initiate a payment that requires user confirmation"""
    elicitation_id = str(uuid.uuid4())

    # 方式 2：使用 ctx.elicit_url（工具执行中途要求用户交互）
    result = await ctx.elicit_url(
        message=f"请确认支付 {amount} {currency}",
        url=f"https://payments.example.com/confirm?amount={amount}&currency={currency}&id={elicitation_id}",
        elicitation_id=elicitation_id,
    )

    if result.action == "accept":
        return f"支付已发起: {amount} {currency}"
    return "支付已取消"
```

### 客户端处理

```python
from mcp.types import ElicitResult

async def handle_elicitation(ctx, params):
    if params.mode == "url":
        print(f"请在浏览器中完成: {params.url}")
        import webbrowser
        webbrowser.open(params.url)
        # 等待用户完成操作...
        return ElicitResult(action="accept")
    # 表单模式
    return ElicitResult(action="accept", content={...})
```

---

## 十二、场景 12：结构化输出 + 低层 Server 的 output_schema

**场景描述**：使用低层 API 手动定义工具的 JSON Schema 输出格式。

### 代码

```python
from mcp import types
from mcp.server import Server
from mcp.server.context import ServerRequestContext

server = Server("structured-output-server")

async def handle_list_tools(ctx, params):
    return types.ListToolsResult(
        tools=[
            types.Tool(
                name="get_stock_price",
                description="Get real-time stock price",
                input_schema={
                    "type": "object",
                    "properties": {
                        "symbol": {"type": "string", "description": "股票代码"},
                    },
                    "required": ["symbol"],
                },
                output_schema={
                    "type": "object",
                    "properties": {
                        "symbol": {"type": "string"},
                        "price": {"type": "number"},
                        "currency": {"type": "string"},
                        "change_pct": {"type": "number"},
                        "timestamp": {"type": "string", "format": "date-time"},
                    },
                    "required": ["symbol", "price", "currency"],
                },
            )
        ]
    )

async def handle_call_tool(ctx, params):
    if params.name == "get_stock_price":
        from datetime import datetime

        symbol = params.arguments["symbol"]
        data = {
            "symbol": symbol,
            "price": 150.25,
            "currency": "USD",
            "change_pct": 2.3,
            "timestamp": datetime.now().isoformat(),
        }

        return types.CallToolResult(
            # 文本内容给 LLM 读
            content=[types.TextContent(
                type="text",
                text=f"{symbol}: ${data['price']} ({data['change_pct']:+.1f}%)",
            )],
            # 结构化内容给程序用（会按 output_schema 验证）
            structured_content=data,
        )

server = Server(
    "structured-output-server",
    on_list_tools=handle_list_tools,
    on_call_tool=handle_call_tool,
)
```

---

## 十三、场景 13：CallToolResult 直接返回 + _meta 元数据

**场景描述**：高层 API 中直接返回 `CallToolResult`，可以附带不暴露给 LLM 的元数据。

### 代码

```python
from typing import Annotated
from pydantic import BaseModel
from mcp.server.mcpserver import MCPServer
from mcp.types import CallToolResult, TextContent

mcp = MCPServer("Meta Demo")

class OrderInfo(BaseModel):
    order_id: str
    status: str
    total: float

@mcp.tool()
def create_order(item: str, quantity: int) -> Annotated[CallToolResult, OrderInfo]:
    """Create a new order"""
    order_id = "ORD-12345"
    total = quantity * 99.9

    return CallToolResult(
        # LLM 看到的文本
        content=[TextContent(type="text", text=f"订单已创建: {order_id}, 总计 ¥{total}")],
        # 结构化数据
        structured_content={
            "order_id": order_id,
            "status": "pending",
            "total": total,
        },
        # 隐藏元数据（不发送给 LLM，但客户端应用可以读取）
        _meta={
            "internal_id": "INT-98765",
            "warehouse": "WH-BEIJING-01",
            "estimated_shipping": "2025-01-18",
            "priority_level": 3,
        },
    )
```

### 客户端使用

```python
result = await client.call_tool("create_order", {"item": "键盘", "quantity": 2})

# LLM 看到：
print(result.content[0].text)
# "订单已创建: ORD-12345, 总计 ¥199.8"

# 程序使用结构化数据：
print(result.structured_content)
# {"order_id": "ORD-12345", "status": "pending", "total": 199.8}

# 客户端应用读取元数据（LLM 不可见）：
print(result.meta)
# {"internal_id": "INT-98765", "warehouse": "WH-BEIJING-01", ...}
```

---

## 十四、场景 14：CORS 配置 —— 浏览器端 MCP 客户端

**场景描述**：MCP 客户端运行在浏览器中（Web 应用），需要配置 CORS 允许跨域请求。

### 代码

```python
from starlette.middleware.cors import CORSMiddleware
from mcp.server.mcpserver import MCPServer

mcp = MCPServer("Browser-Friendly Server")

@mcp.tool()
def search(query: str) -> str:
    return f"Results for: {query}"

# 获取 Starlette 应用
starlette_app = mcp.streamable_http_app(
    stateless_http=True,
    json_response=True,
)

# 添加 CORS 中间件
app = CORSMiddleware(
    starlette_app,
    allow_origins=["http://localhost:3000", "https://myapp.com"],
    allow_methods=["GET", "POST", "DELETE", "OPTIONS"],
    allow_headers=["Content-Type", "Authorization", "Mcp-Session-Id"],
    # 关键：暴露 MCP Session ID 头
    expose_headers=["Mcp-Session-Id"],
    allow_credentials=True,
)

# 启动: uvicorn main:app --host 0.0.0.0 --port 8000
```

**要点**：
- `expose_headers=["Mcp-Session-Id"]` 是必须的，浏览器需要读取这个头
- `allow_methods` 必须包含 `DELETE`（用于关闭 session）
- 生产环境不要使用 `allow_origins=["*"]`

---

## 十五、场景 15：安全传输 —— DNS 重绑定防护

**场景描述**：防止 DNS 重绑定攻击，确保只有合法的请求能到达 MCP 服务器。

### 代码

```python
from mcp.server.mcpserver import MCPServer
from mcp.server.transport_security import TransportSecuritySettings

mcp = MCPServer("Secure Server")

@mcp.tool()
def sensitive_op(data: str) -> str:
    return f"processed: {data}"

if __name__ == "__main__":
    mcp.run(
        transport="streamable-http",
        host="127.0.0.1",
        port=8000,
        transport_security=TransportSecuritySettings(
            # 允许的 Host 头列表
            allowed_hosts=["localhost", "127.0.0.1"],
            # 允许的 Origin 列表（用于 CORS 检查）
            allowed_origins=["http://localhost:3000"],
        ),
    )
```

### 效果

```
# 合法请求
POST http://127.0.0.1:8000/mcp
Host: 127.0.0.1:8000
→ 200 OK

# DNS 重绑定攻击
POST http://127.0.0.1:8000/mcp
Host: evil.example.com     ← Host 不在允许列表中
→ 403 Forbidden
```

---

## 十六、架构模式总结

### 层次架构

```
┌──────────────────────────────────────────────────┐
│                    客户端应用                       │
│  (Claude Desktop / Web App / CLI / 自定义应用)       │
├──────────────────────────────────────────────────┤
│  Client (高层)  或  ClientSession (底层)            │
│  └── ClientSessionGroup (多服务器聚合)              │
├──────────────────────────────────────────────────┤
│              传输层 (Transport)                     │
│  stdio_client │ streamable_http_client │ sse_client │
├──────────────────────────────────────────────────┤
│              网络 / 进程管道                         │
├──────────────────────────────────────────────────┤
│              传输层 (Transport)                     │
│  stdio_server │ StreamableHTTP │ SSE               │
├──────────────────────────────────────────────────┤
│  MCPServer (高层)  或  Server (底层)                │
│  ├── Tool (工具)                                   │
│  ├── Resource (资源)                               │
│  ├── Prompt (提示)                                 │
│  ├── Context (上下文)                              │
│  ├── Elicitation (用户交互)                         │
│  ├── Sampling (LLM 调用)                           │
│  ├── Task (异步任务)                               │
│  └── Auth (OAuth 认证)                             │
└──────────────────────────────────────────────────┘
```

### 选型决策树

```
需要什么级别的控制？
├── 快速开发 → MCPServer (高层装饰器 API)
│   ├── 简单本地工具 → stdio 传输
│   ├── Web 服务 → Streamable HTTP (stateless)
│   └── 需要实时推送 → Streamable HTTP (stateful + EventStore)
│
├── 完全控制 → Server (低层回调 API)
│   ├── 自定义分页 → on_list_tools/on_list_resources
│   ├── 自定义 schema → 手写 input_schema/output_schema
│   └── 自定义协议 → 直接操作 read_stream/write_stream
│
├── 需要认证 → MCPServer + AuthSettings + TokenVerifier
│
├── 长任务 → Task 模式 (experimental)
│   ├── 简单轮询 → server.experimental.enable_tasks() + run_task()
│   └── 交互式 → task.elicit() + task.create_message()
│
├── 多服务器 → ClientSessionGroup
│
└── 现有框架集成 → ASGI 挂载 (mcp.streamable_http_app())
    ├── Starlette → Mount("/mcp", app=...)
    └── FastAPI → app.mount("/mcp", ...)
```
