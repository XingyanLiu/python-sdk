# MCP Python SDK 高频接口与使用场景

> 本文档面向已入门的 MCP SDK 开发者，整理高频使用的接口、常见使用场景及完整交互示例。

---

## 目录

- [一、高频接口速查表](#一高频接口速查表)
- [二、场景 1：Context 上下文 —— 进度报告与日志](#二场景-1context-上下文--进度报告与日志)
- [三、场景 2：参数描述与校验 —— Pydantic Field](#三场景-2参数描述与校验--pydantic-field)
- [四、场景 3：Elicitation —— 向用户请求输入](#四场景-3elicitation--向用户请求输入)
- [五、场景 4：Sampling —— 从工具内部调用 LLM](#五场景-4sampling--从工具内部调用-llm)
- [六、场景 5：Lifespan —— 管理数据库等外部资源的生命周期](#六场景-5lifespan--管理数据库等外部资源的生命周期)
- [七、场景 6：Image 处理 —— 返回图片数据](#七场景-6image-处理--返回图片数据)
- [八、场景 7：动态增删 Tool](#八场景-7动态增删-tool)
- [九、场景 8：变更通知 —— 通知客户端资源/工具更新](#九场景-8变更通知--通知客户端资源工具更新)
- [十、场景 9：SSE 传输 + Streamable HTTP 选型](#十场景-9sse-传输--streamable-http-选型)
- [十一、场景 10：单元测试 —— 用 Client 直连测试服务器](#十一场景-10单元测试--用-client-直连测试服务器)
- [十二、场景 11：多服务器聚合 —— ClientSessionGroup](#十二场景-11多服务器聚合--clientsessiongroup)
- [十三、场景 12：Completions —— 自动补全支持](#十三场景-12completions--自动补全支持)

---

## 一、高频接口速查表

### 服务端 Context API（`ctx: Context`）

在任何 `@mcp.tool()` 或 `@mcp.resource()` 函数中，添加 `ctx: Context` 参数即可自动注入。

| 方法 | 说明 | 使用频率 |
|---|---|---|
| `await ctx.info(data)` | 发送 INFO 级别日志 | ⭐⭐⭐ |
| `await ctx.debug(data)` | 发送 DEBUG 级别日志 | ⭐⭐⭐ |
| `await ctx.warning(data)` | 发送 WARNING 级别日志 | ⭐⭐ |
| `await ctx.error(data)` | 发送 ERROR 级别日志 | ⭐⭐ |
| `await ctx.report_progress(progress, total, message)` | 报告进度 | ⭐⭐⭐ |
| `await ctx.read_resource(uri)` | 读取其他 Resource | ⭐⭐ |
| `await ctx.elicit(message, schema)` | 向用户请求结构化输入 | ⭐⭐ |
| `await ctx.elicit_url(message, url, id)` | 向用户请求 URL 交互 | ⭐ |
| `ctx.session` | 底层 ServerSession 引用 | ⭐⭐ |
| `ctx.request_context.lifespan_context` | 获取 Lifespan 上下文 | ⭐⭐ |
| `ctx.request_id` | 当前请求 ID | ⭐ |
| `ctx.client_id` | 客户端标识 | ⭐ |

### 客户端 Session API

| 方法 | 说明 | 使用频率 |
|---|---|---|
| `await session.initialize()` | 协议握手（必须首先调用） | ⭐⭐⭐ |
| `await session.list_tools()` | 列出所有工具 | ⭐⭐⭐ |
| `await session.call_tool(name, args)` | 调用工具 | ⭐⭐⭐ |
| `await session.list_resources()` | 列出所有资源 | ⭐⭐⭐ |
| `await session.read_resource(uri)` | 读取资源 | ⭐⭐⭐ |
| `await session.list_prompts()` | 列出所有提示 | ⭐⭐ |
| `await session.get_prompt(name, args)` | 获取提示实例 | ⭐⭐ |
| `await session.list_resource_templates()` | 列出资源模板 | ⭐⭐ |
| `await session.complete(ref, argument)` | 自动补全 | ⭐ |
| `await session.send_ping()` | 心跳检测 | ⭐ |
| `await session.set_logging_level(level)` | 设置日志级别 | ⭐ |

---

## 二、场景 1：Context 上下文 —— 进度报告与日志

### 🎯 业务场景

> **故事**：数据团队开发了一个"批量处理 CSV 文件"的 MCP 工具。问题是：处理 500 个文件需要 5 分钟，用户在 AI 对话界面干等着，不知道卡没卡、进度到哪了。产品经理要求："至少得有个进度条吧！另外后台在处理哪个文件，最好也能实时显示。"
>
> **直观理解**：这就像你叫了外卖——
> - **没有进度报告**：你只知道"已下单"，然后就是漫长的等待，不知道骑手到哪了 😰
> - **有进度报告**：App 实时显示"商家已接单 → 骑手已取餐 → 距你 1.2km → 已送达" 🎉
>
> MCP 的 `Context` 就是给工具提供了一个"外卖 App 的实时追踪系统"。工具可以在执行过程中随时上报进度和日志，客户端实时展示。
>
> **解决的痛点**：长时间运行的工具不再是黑盒——用户能看到进度、开发者能看到日志。

### 服务端代码

```python
import anyio
from mcp.server.mcpserver import MCPServer, Context

mcp = MCPServer("Data Processor")

@mcp.tool()
async def process_files(directory: str, ctx: Context) -> str:
    """Process all files in a directory"""
    files = ["data_01.csv", "data_02.csv", "data_03.csv", "data_04.csv", "data_05.csv"]
    total = len(files)
    results = []

    await ctx.info(f"开始处理目录: {directory}")

    for i, filename in enumerate(files):
        # 报告进度
        await ctx.report_progress(
            progress=i,
            total=total,
            message=f"正在处理: {filename}",
        )

        await ctx.debug(f"读取文件 {filename}...")
        # 模拟处理耗时
        await anyio.sleep(0.5)

        results.append(f"{filename}: OK")
        await ctx.info(f"完成处理: {filename} ({i + 1}/{total})")

    await ctx.report_progress(progress=total, total=total, message="全部完成")
    return f"处理完成，共 {total} 个文件:\n" + "\n".join(results)
```

### 客户端代码（带进度回调）

```python
import anyio
from mcp.client.client import Client
from mcp.server.mcpserver import MCPServer

async def main():
    async with Client(mcp) as client:
        # call_tool 支持 progress_callback
        def on_progress(progress, total, message):
            pct = (progress / total * 100) if total else 0
            print(f"[进度] {pct:.0f}% - {message}")

        result = await client.call_tool(
            "process_files",
            {"directory": "/data"},
            progress_callback=on_progress,
        )
        print(f"\n最终结果:\n{result.content[0].text}")

anyio.run(main)
```

### 交互输出

用户在 AI 界面看到的效果（客户端实时展示进度）：

```
[进度] 0% - 正在处理: data_01.csv      ← 刚开始，用户看到"哦，在处理第一个文件"
[进度] 20% - 正在处理: data_02.csv     ← 20% 了，还不错
[进度] 40% - 正在处理: data_03.csv     ← 快一半了
[进度] 60% - 正在处理: data_04.csv     ← 过半了，心里有底
[进度] 80% - 正在处理: data_05.csv     ← 马上完成
[进度] 100% - 全部完成                 ← 🎉 搞定！

最终结果:
处理完成，共 5 个文件:
data_01.csv: OK
data_02.csv: OK
data_03.csv: OK
data_04.csv: OK
data_05.csv: OK
```

**端到端流程图**（展示数据流向）：

```
用户 ──"帮我处理 /data 下的所有文件"──→ AI (LLM)
                                          │
                                          ▼
                             AI 调用 process_files("/data")
                                          │
                                          ▼
                                    MCP 工具开始执行
                                    ┌─────────────┐
                                    │ 处理 01.csv  │──→ 进度 0% ──→ 客户端实时显示
                                    │ 处理 02.csv  │──→ 进度 20% ──→ 客户端实时显示
                                    │ 处理 03.csv  │──→ 进度 40% ──→ 客户端实时显示
                                    │ ...          │
                                    │ 全部完成     │──→ 进度 100%
                                    └─────────────┘
                                          │
                                          ▼
                                返回最终结果给 AI
                                          │
                                          ▼
AI ──"处理完成！5 个文件全部成功。"──→ 用户
```

> **给老板看的一句话总结**：现在用户让 AI 处理文件时，能实时看到进度条和当前在处理哪个文件，不再干等了。

---

## 三、场景 2：参数描述与校验 —— Pydantic Field

### 🎯 业务场景

> **故事**：团队做了一个"发邮件"的工具和"查数据库"的工具。但测试时发现 AI 经常搞混参数——比如把邮件主题和正文搞反，或者不知道 priority 字段应该填 1-5 的整数。原因是参数名不够直观，缺少说明信息。
>
> **直观理解**：就像你填一个在线表单——
> - **没有描述的表单**：只有输入框，没有提示文字。你对着一个空的 "to" 字段发懵：填人名？填邮箱？填部门？
> - **有描述的表单**：每个输入框下面都有灰色提示 "请输入收件人邮箱地址，如 xxx@company.com"。一看就知道该填什么。
>
> `Pydantic Field` 就是给工具参数加上"表单提示文字"，让 AI 也能准确理解每个参数该填什么。
>
> **解决的痛点**：AI 不再猜参数含义，填参数的准确率大幅提升。同时还能设置默认值、类型约束。

### 代码

```python
from pydantic import BaseModel, Field
from mcp.server.mcpserver import MCPServer

mcp = MCPServer("Email Service")

@mcp.tool()
def send_email(
    to: str = Field(description="收件人邮箱地址"),
    subject: str = Field(description="邮件主题"),
    body: str = Field(description="邮件正文内容"),
    cc: str = Field(description="抄送邮箱（可选）", default=""),
    priority: int = Field(description="优先级 1-5，1 最高", default=3),
) -> str:
    """Send an email to the specified recipient"""
    result = f"邮件已发送:\n  收件人: {to}\n  主题: {subject}\n  优先级: {priority}"
    if cc:
        result += f"\n  抄送: {cc}"
    return result

# 使用 Pydantic BaseModel 定义复杂输入
class DatabaseQuery(BaseModel):
    table: str = Field(description="表名")
    conditions: dict[str, str] = Field(description="查询条件", default_factory=dict)
    limit: int = Field(description="返回记录数上限", default=10)
    order_by: str = Field(description="排序字段", default="id")

@mcp.tool()
def query_database(query: DatabaseQuery) -> str:
    """Execute a database query"""
    return (
        f"查询表 {query.table}，条件 {query.conditions}，"
        f"限制 {query.limit} 条，按 {query.order_by} 排序"
    )
```

### 交互示例

看看 AI 看到的工具 "说明书" 和实际调用过程：

```
# === AI 看到的工具参数 Schema（就像一张参数说明卡片） ===
# ┌─────────────────────────────────────────────────────┐
# │ 工具: send_email                                     │
# │ 说明: Send an email to the specified recipient       │
# ├─────────────────────────────────────────────────────┤
# │ 参数:                                                │
# │   to (string, 必填): "收件人邮箱地址"                 │
# │   subject (string, 必填): "邮件主题"                  │
# │   body (string, 必填): "邮件正文内容"                 │
# │   cc (string, 可选, 默认""): "抄送邮箱（可选）"       │
# │   priority (integer, 可选, 默认3): "优先级1-5,1最高"  │
# └─────────────────────────────────────────────────────┘
# AI 看到这些描述后，就知道了：
# - "to" 要填邮箱，不是人名
# - "priority" 是 1-5 的数字，1 最紧急
# - "cc" 可以不填

# === 实际调用场景：用户说"帮我给 user@example.com 发一封紧急邮件" ===
await client.call_tool("send_email", {
    "to": "user@example.com",     # AI 正确填了邮箱
    "subject": "项目进展",         # AI 自己组织了主题
    "body": "本周已完成...",       # AI 自己组织了正文
    "priority": 1,                # AI 知道"紧急"对应 priority=1
})
# 返回: "邮件已发送:\n  收件人: user@example.com\n  主题: 项目进展\n  优先级: 1"

# === 复杂输入场景：用户说"帮我查一下活跃用户，只要前5条" ===
await client.call_tool("query_database", {
    "query": {
        "table": "users",
        "conditions": {"status": "active"},
        "limit": 5,
        # order_by 没传 → 自动使用默认值 "id"
    }
})
# 返回: "查询表 users，条件 {'status': 'active'}，限制 5 条，按 id 排序"
```

> **给老板看的一句话总结**：给每个参数加了中文说明后，AI 填参数的准确率从 70% 提升到了 95%+。

---

## 四、场景 3：Elicitation —— 向用户请求输入

### 🎯 业务场景

> **故事**：你做了一个"餐厅预订"工具。AI 帮用户查到了一个空桌位，但在真正下单之前需要用户亲自确认——因为这涉及到扣费。你不能让 AI 自己做决定。
>
> **直观理解**：就像网购付款——你在淘宝选好了商品、填好了地址，最后一步系统会弹出一个确认框："确认支付 ¥800 吗？[确认] [取消]"。Elicitation 就是 MCP 版的"确认框"：工具执行到一半，暂停下来，弹出一个表单让用户填写确认信息，用户填完后工具继续执行。
>
> **关键词解释**：Elicitation = "引出、诱导用户输入"。在 MCP 里就是"工具执行到一半，主动问用户一个问题"。
>
> **解决的痛点**：涉及付费、删除、提交等不可逆操作时，工具不会"自作主张"，而是先征求用户同意。

### 代码

```python
from pydantic import BaseModel, Field
from mcp.server.mcpserver import MCPServer, Context

mcp = MCPServer("Booking Service")

class BookingConfirmation(BaseModel):
    confirmed: bool = Field(description="是否确认预订？")
    special_requests: str = Field(description="特殊要求", default="")

@mcp.tool()
async def book_restaurant(
    restaurant: str,
    date: str,
    guests: int,
    ctx: Context,
) -> str:
    """Book a table at a restaurant"""
    await ctx.info(f"查询 {restaurant} 在 {date} 的可用性...")

    # 模拟查询
    price = guests * 200

    # 向用户请求确认
    result = await ctx.elicit(
        message=f"找到可用桌位！{restaurant}，{date}，{guests} 位客人，预计消费 ¥{price}。请确认预订：",
        schema=BookingConfirmation,
    )

    if result.action == "accept" and result.data:
        msg = f"预订成功！{restaurant}，{date}，{guests} 人"
        if result.data.special_requests:
            msg += f"\n特殊要求: {result.data.special_requests}"
        return msg
    elif result.action == "decline":
        return "用户取消了预订"
    else:
        return "操作已取消"
```

### 交互流程

完整的餐厅预订流程，从用户说话到订单完成：

```
👤 用户在 AI 对话框里说：
   "帮我在'老北京'饭店预订明天 4 人的位置"

   ┌──────────────────────────── AI 内部流程 ────────────────────────────┐
   │                                                                      │
   │  🤖 AI (LLM) 理解用户意图，调用预订工具：                            │
   │     call_tool("book_restaurant", {                                   │
   │         "restaurant": "老北京",                                      │
   │         "date": "2025-01-16",                                        │
   │         "guests": 4                                                  │
   │     })                                                               │
   │                                                                      │
   │  🔧 工具开始执行：                                                   │
   │     1. 查询老北京饭店 1月16日 的空位 → 找到了！                       │
   │     2. 计算预计费用：4人 × ¥200 = ¥800                               │
   │     3. ⚠️ 但是！不能直接预订，需要用户确认                           │
   │                                                                      │
   │  📋 工具暂停，弹出确认表单（Elicitation）：                           │
   │     ┌─────────────────────────────────────────┐                      │
   │     │ 找到可用桌位！                           │                      │
   │     │ 老北京，2025-01-16，4位客人              │                      │
   │     │ 预计消费 ¥800                            │                      │
   │     │                                         │                      │
   │     │ 是否确认预订？ [✅ 是] [❌ 否]           │                      │
   │     │ 特殊要求：[__靠窗位置__________]         │                      │
   │     └─────────────────────────────────────────┘                      │
   │                                                                      │
   │  👤 用户点击确认，填写 "靠窗位置"                                    │
   │                                                                      │
   │  🔧 工具收到确认，继续执行：                                         │
   │     → 生成预订订单                                                   │
   │     → 返回 "预订成功！老北京，2025-01-16，4人"                       │
   │                                                                      │
   └──────────────────────────────────────────────────────────────────────┘

🤖 AI 最终回复用户：
   "已经帮您在老北京饭店预订好了明天的4人桌，靠窗位置。预计消费 ¥800。"
```

> **给老板看的一句话总结**：AI 在帮用户做重要操作（如付款、预订）前，会先弹出确认框让用户自己决定，不会擅自做主。

---

## 五、场景 4：Sampling —— 从工具内部调用 LLM

### 🎯 业务场景

> **故事**：你做了一个"客服工单处理"工具。工具收到一个很长的客户投诉后，需要先用 AI 把投诉内容总结成一句话，然后再分类（查询/投诉/建议/闲聊）。问题是——工具本身不是 AI，它只是一段 Python 代码，怎么在工具里面调用 AI 的能力？
>
> **直观理解**：想象一个公司里的流程——
> - 用户（客户）→ 打电话给**前台（AI）** → 前台让**行政部（工具）**处理
> - 但行政部发现需要**法务部（另一个 AI）** 给一个意见
> - 所以行政部通过前台**反过来问法务部**一个问题
>
> Sampling 就是"工具反过来请求 AI 帮忙"。数据流向是反的：正常是 AI→工具，Sampling 是 工具→AI→工具。
>
> **关键词解释**：Sampling 字面意思是"采样"，在 MCP 里指"工具请求客户端用 LLM 生成一段文本"。
>
> **解决的痛点**：工具可以在执行过程中借助 AI 的理解能力来处理非结构化数据（翻译、摘要、分类等）。

### 代码

```python
from mcp.server.mcpserver import MCPServer, Context
from mcp.types import SamplingMessage, TextContent

mcp = MCPServer("Smart Assistant")

@mcp.tool()
async def summarize_text(text: str, max_words: int, ctx: Context) -> str:
    """Summarize a long text using LLM"""
    await ctx.info("正在使用 LLM 生成摘要...")

    # 通过 session.create_message 向客户端请求 LLM 推理
    result = await ctx.session.create_message(
        messages=[
            SamplingMessage(
                role="user",
                content=TextContent(
                    type="text",
                    text=f"请用不超过 {max_words} 个字总结以下文本：\n\n{text}",
                ),
            )
        ],
        max_tokens=500,
    )

    if result.content.type == "text":
        return f"摘要：{result.content.text}"
    return "无法生成摘要"

@mcp.tool()
async def classify_intent(user_message: str, ctx: Context) -> str:
    """Classify user intent into categories"""
    result = await ctx.session.create_message(
        messages=[
            SamplingMessage(
                role="user",
                content=TextContent(
                    type="text",
                    text=(
                        "将以下用户消息分类为以下类别之一："
                        "查询、投诉、建议、闲聊。\n\n"
                        f"用户消息：{user_message}\n\n"
                        "请只返回类别名称。"
                    ),
                ),
            )
        ],
        max_tokens=50,
    )

    if result.content.type == "text":
        return f"意图分类：{result.content.text.strip()}"
    return "分类失败"
```

### 交互流程

以"文章摘要"为例，看看数据如何在用户、AI、工具之间流转：

```
👤 用户："帮我总结这篇 3000 字的文章"
    │
    ▼
🤖 AI (LLM)："好的，我来调用摘要工具"
    │
    ▼  call_tool("summarize_text", {"text": "..3000字..", "max_words": 50})
    │
🔧 工具（summarize_text）开始执行：
    │  "嗯，要把 3000 字总结成 50 字，我自己做不到"
    │  "让我请 AI 帮忙！"
    │
    ▼  🔄 Sampling 请求（工具 → 客户端 → AI）
    │  "请用不超过 50 字总结以下文本：..."
    │
    │  客户端收到 Sampling 请求，调用 LLM API：
    │     LLM 回答："这篇文章主要讨论了人工智能在医疗领域的应用前景..."
    │
    ▼  🔄 Sampling 响应（AI → 客户端 → 工具）
    │
🔧 工具收到 AI 的回答，组织最终结果：
    │  "摘要：这篇文章主要讨论了人工智能在医疗领域的应用前景..."
    │
    ▼
🤖 AI (LLM) 收到工具结果，生成自然语言回复：
    │
    ▼
👤 用户看到：
   "这篇文章的核心内容是：人工智能在医疗领域的应用前景，
    特别是在影像诊断和药物研发方面的突破。"
```

**注意**：客户端必须提供 `sampling_callback` 才能处理 Sampling 请求（因为是客户端去调用 LLM API）：

```python
async def handle_sampling(ctx, params):
    # 这里调用实际的 LLM API（如 OpenAI、Claude 等）
    response = await call_llm(params.messages, params.max_tokens)
    return CreateMessageResult(
        role="assistant",
        content=TextContent(type="text", text=response),
        model="gpt-4",
    )

async with Client(mcp, sampling_callback=handle_sampling) as client:
    result = await client.call_tool("summarize_text", {"text": "...", "max_words": 50})
```

> **给老板看的一句话总结**：工具在执行过程中可以"借用" AI 的大脑来处理复杂文本（翻译、摘要、分类），实现"AI 里面再套 AI"。

---

## 六、场景 5：Lifespan —— 管理数据库等外部资源的生命周期

### 🎯 业务场景

> **故事**：你的 MCP 工具需要查数据库。问题是：每次调用工具都新建一个数据库连接太慢了（连接需要 200ms），而且连接数会爆掉。你需要在服务器启动时建好连接池，所有工具共用，服务器关闭时再释放。
>
> **直观理解**：就像开一家餐厅——
> - **开店前**（Lifespan 启动）：先备好食材、热好炉子、准备好餐具
> - **营业中**（工具被调用）：直接用备好的东西做菜，不用每道菜都重新采购食材
> - **打烊后**（Lifespan 关闭）：关炉子、收拾餐具、存好剩余食材
>
> Lifespan 就是"餐厅的开店/打烊流程"——在服务器启动时初始化所有共享资源，关闭时统一清理。
>
> **解决的痛点**：数据库连接、缓存客户端、API Token 等只需初始化一次，所有工具共享使用，性能好且不会泄漏资源。

### 代码

```python
from collections.abc import AsyncIterator
from contextlib import asynccontextmanager
from dataclasses import dataclass
from mcp.server.mcpserver import MCPServer, Context

# 模拟数据库客户端
class Database:
    def __init__(self, url: str):
        self.url = url
        self.connected = False

    @classmethod
    async def connect(cls, url: str) -> "Database":
        db = cls(url)
        db.connected = True
        print(f"数据库已连接: {url}")
        return db

    async def query(self, sql: str) -> list[dict]:
        return [{"id": 1, "name": "示例数据"}]

    async def disconnect(self) -> None:
        self.connected = False
        print("数据库已断开")

# 模拟缓存客户端
class Cache:
    @classmethod
    async def create(cls) -> "Cache":
        print("缓存已初始化")
        return cls()

    async def close(self) -> None:
        print("缓存已关闭")

@dataclass
class AppContext:
    db: Database
    cache: Cache

@asynccontextmanager
async def app_lifespan(server: MCPServer) -> AsyncIterator[AppContext]:
    """服务器启动时初始化资源，关闭时清理"""
    db = await Database.connect("postgresql://localhost/mydb")
    cache = await Cache.create()
    try:
        yield AppContext(db=db, cache=cache)
    finally:
        await cache.close()
        await db.disconnect()

mcp = MCPServer("Database Service", lifespan=app_lifespan)

@mcp.tool()
async def query_users(limit: int, ctx: Context) -> str:
    """Query users from database"""
    # 从 lifespan context 获取数据库实例
    app: AppContext = ctx.request_context.lifespan_context
    rows = await app.db.query(f"SELECT * FROM users LIMIT {limit}")
    return f"查询到 {len(rows)} 条记录: {rows}"

@mcp.resource("status://database")
def database_status(ctx: Context) -> str:
    """Check database connection status"""
    app: AppContext = ctx.request_context.lifespan_context
    return f"数据库连接状态: {'已连接' if app.db.connected else '未连接'}"
```

### 交互流程

完整的服务器生命周期：

```
# ===== 阶段1：服务器启动（Lifespan 初始化） =====
$ python db_server.py
数据库已连接: postgresql://localhost/mydb    ← 🔌 数据库连接建好了
缓存已初始化                                  ← 📦 缓存也准备好了
# 此时 AppContext(db=..., cache=...) 被创建，所有工具都可以使用

# ===== 阶段2：营业中（工具正常服务） =====
# 用户通过 AI 说："帮我查一下用户列表"

await client.call_tool("query_users", {"limit": 10})
# → 工具从 ctx 中取出数据库实例（不用重新连接！）
# → 执行 SQL 查询
# → 返回: "查询到 1 条记录: [{'id': 1, 'name': '示例数据'}]"

# 用户又说："数据库连接正常吗？"

await client.read_resource("status://database")
# → 读取同一个数据库实例的状态
# → 返回: "数据库连接状态: 已连接"

# 无论工具被调用多少次，都复用同一个数据库连接 ✅

# ===== 阶段3：服务器关闭（Lifespan 清理） =====
# Ctrl+C 停止服务器
缓存已关闭                                    ← 📦 缓存优雅关闭
数据库已断开                                  ← 🔌 数据库连接释放
# 没有资源泄漏 ✅
```

> **给老板看的一句话总结**：数据库连接只建一次，所有请求共享使用，服务器关闭时自动释放——既快又安全。

---

## 七、场景 6：Image 处理 —— 返回图片数据

### 🎯 业务场景

> **故事**：数据分析团队希望用户在 AI 对话中说"帮我画一张这个月的销售趋势图"，AI 就能生成一张折线图并直接显示在对话窗口里——就像在微信里发图片一样。
>
> **直观理解**：普通工具返回的是"文字"，但有时候一图胜千言。`Image` 类型让工具可以返回图片——AI 对话界面会直接渲染出来，就像 AI 在聊天框里"发了一张图"。
>
> **解决的痛点**：数据可视化、截图、缩略图等图片类结果可以直接在 AI 对话中展示，无需让用户去别的地方查看。

### 代码

```python
from mcp.server.mcpserver import MCPServer, Image

mcp = MCPServer("Image Service")

@mcp.tool()
def generate_chart(title: str, data_points: list[float]) -> Image:
    """Generate a simple chart image"""
    # 使用 matplotlib 生成图表
    import io
    import matplotlib
    matplotlib.use("Agg")
    import matplotlib.pyplot as plt

    fig, ax = plt.subplots()
    ax.plot(data_points)
    ax.set_title(title)

    buf = io.BytesIO()
    fig.savefig(buf, format="png")
    buf.seek(0)
    plt.close(fig)

    return Image(data=buf.read(), format="png")

@mcp.tool()
def create_thumbnail(width: int, height: int) -> Image:
    """Create a placeholder thumbnail"""
    from PIL import Image as PILImage

    img = PILImage.new("RGB", (width, height), color=(73, 109, 137))
    buf = io.BytesIO()
    img.save(buf, format="JPEG")
    buf.seek(0)

    return Image(data=buf.read(), format="jpeg")
```

### 交互示例

用户和 AI 的对话场景：

```
👤 用户："帮我画一张最近7天的销售趋势图"

🤖 AI 调用工具：
   call_tool("generate_chart", {
       "title": "销售趋势",
       "data_points": [10, 25, 18, 35, 42, 38, 55]
   })

🔧 工具执行：
   1. 用 matplotlib 生成折线图
   2. 保存为 PNG 格式的字节流
   3. 包装成 Image(data=png_bytes, format="png")
   4. SDK 自动将图片数据编码为 base64

📦 返回结果：
   result.content[0] 是 ImageContent 类型
   result.content[0].data → base64 编码的 PNG 数据
   result.content[0].mimeType → "image/png"

💬 AI 对话界面直接显示图片：
   ┌─────────────────────────────┐
   │     📈 销售趋势              │
   │   55 ·                  ╱   │
   │   42 ·            ╱───╱    │
   │   35 ·      ╱───╱         │
   │   25 · ╱──╱               │
   │   10 ·╱                    │
   │     Day1 Day2 ... Day7     │
   └─────────────────────────────┘

🤖 AI 同时回复："这是最近7天的销售趋势图。可以看到整体呈上升趋势，
   第7天达到最高值 55。需要我分析一下具体数据吗？"
```

> **给老板看的一句话总结**：用户让 AI 画图，AI 就能在对话框里直接显示图表——数据可视化不再需要打开另一个软件。

---

## 八、场景 7：动态增删 Tool

### 🎯 业务场景

> **故事**：你做的 AI 助手有普通模式和管理员模式。普通用户只能搜索，管理员输入密码后才能看到"删除"功能。就像游戏里的隐藏关卡——你不输入秘籍，就看不到隐藏角色。
>
> **直观理解**：想象一个自助取款机——
> - 普通界面：查余额、取款、转账
> - 输入管理员密码后：多出了"清机"、"加钞"、"维护"等隐藏选项
> - 退出管理员模式：隐藏选项消失，恢复普通界面
>
> **解决的痛点**：根据用户身份/权限/场景动态展示不同的工具集，而不是一股脑全暴露。

### 代码

```python
from mcp.server.mcpserver import MCPServer, Context

mcp = MCPServer("Dynamic Service")

@mcp.tool()
def basic_search(query: str) -> str:
    """Basic search available to all users"""
    return f"搜索结果: {query}"

# 动态添加 admin 工具
def admin_delete(item_id: str) -> str:
    """Delete an item (admin only)"""
    return f"已删除: {item_id}"

@mcp.tool()
async def enable_admin_mode(password: str, ctx: Context) -> str:
    """Enable admin tools"""
    if password == "admin123":
        mcp.add_tool(admin_delete, name="admin_delete", description="Delete items")
        # 通知客户端工具列表已更新
        await ctx.session.send_tool_list_changed()
        return "Admin 模式已启用"
    return "密码错误"

@mcp.tool()
async def disable_admin_mode(ctx: Context) -> str:
    """Disable admin tools"""
    mcp.remove_tool("admin_delete")
    await ctx.session.send_tool_list_changed()
    return "Admin 模式已禁用"
```

### 交互流程

完整的"解锁管理员功能"流程：

```
# ===== 1. 初始状态：普通用户只看到 3 个工具 =====
await client.list_tools()
# → [basic_search, enable_admin_mode, disable_admin_mode]
# AI 告诉用户："我可以帮你搜索内容。如果你是管理员，可以解锁更多功能。"

# ===== 2. 用户说"我要进入管理员模式"，输入密码 =====
await client.call_tool("enable_admin_mode", {"password": "admin123"})
# → "Admin 模式已启用"
# ⚡ 此时服务器做了两件事：
#   a) 调用 mcp.add_tool() 注册了新工具 "admin_delete"
#   b) 调用 send_tool_list_changed() 通知客户端："工具列表变了，快刷新！"

# ===== 3. 客户端收到通知，刷新工具列表 =====
await client.list_tools()
# → [basic_search, enable_admin_mode, disable_admin_mode, admin_delete]  ← 多了 admin_delete！
# AI 现在知道："我还可以帮用户删除内容了"

# ===== 4. 管理员执行删除操作 =====
await client.call_tool("admin_delete", {"item_id": "item-42"})
# → "已删除: item-42"

# ===== 5. 管理员退出管理模式 =====
await client.call_tool("disable_admin_mode", {})
# → "Admin 模式已禁用"
# ⚡ admin_delete 工具被移除，客户端收到通知
# 再查工具列表，又回到了 3 个
```

> **给老板看的一句话总结**：AI 助手可以根据用户权限动态显示不同功能——普通用户只能搜索，管理员还能删除。

---

## 九、场景 8：变更通知 —— 通知客户端资源/工具更新

### 🎯 业务场景

> **故事**：你做了一个"在线文档协作"的 MCP 工具。后台数据经常被其他用户修改，但 AI 客户端还在读旧数据。你需要一种机制：服务器数据变了，能主动通知所有连着的客户端"喂，数据更新了，快来重新读一下"。
>
> **直观理解**：就像微信群消息——
> - **没有通知**：你必须不停刷新群聊才能看到新消息
> - **有通知**：手机锁屏上弹出"张三在群里发了一条消息"，你点进去就能看到最新内容
>
> MCP 的变更通知就是这个"消息推送"机制：服务器数据变化时，主动 push 一条通知给客户端。
>
> **解决的痛点**：客户端不需要轮询，数据更新时自动收到通知并刷新。

### 代码

```python
from mcp.server.mcpserver import MCPServer, Context

mcp = MCPServer("Notification Demo")

data_store: dict[str, str] = {"key1": "value1"}

@mcp.resource("data://{key}")
def read_data(key: str) -> str:
    return data_store.get(key, "not found")

@mcp.tool()
async def update_data(key: str, value: str, ctx: Context) -> str:
    """Update a data entry and notify clients"""
    data_store[key] = value

    # 通知客户端该资源已更新
    from pydantic import AnyUrl
    await ctx.session.send_resource_updated(AnyUrl(f"data://{key}"))

    # 如果是新 key，通知资源列表变化
    await ctx.session.send_resource_list_changed()

    return f"已更新: {key} = {value}"

@mcp.tool()
async def batch_update(updates: dict[str, str], ctx: Context) -> str:
    """Batch update multiple entries"""
    for key, value in updates.items():
        data_store[key] = value
        from pydantic import AnyUrl
        await ctx.session.send_resource_updated(AnyUrl(f"data://{key}"))

    await ctx.session.send_resource_list_changed()
    return f"批量更新了 {len(updates)} 条记录"
```

### 交互流程

一个完整的"数据更新 → 通知 → 刷新"流程：

```
# ===== 前置：客户端订阅某个资源（"我关注这个数据，变了告诉我"） =====
await session.subscribe_resource("data://key1")
# 类似于：关注了某个文档的更新提醒

# ===== 场景A：单条更新 =====
# 另一个用户（或定时任务）通过 AI 修改了数据
await client.call_tool("update_data", {"key": "key1", "value": "new_value"})
# 工具内部做了三件事：
#   1️⃣ 更新了 data_store["key1"] = "new_value"
#   2️⃣ 发送 ResourceUpdated 通知 → "key1 的内容变了"
#   3️⃣ 发送 ResourceListChanged 通知 → "资源列表可能变了"
#
# 🔔 客户端收到通知！
# 客户端自动重新读取：
await client.read_resource("data://key1")
# → 拿到了最新的 "new_value"（而不是旧的 "value1"）

# ===== 场景B：批量更新 =====
await client.call_tool("batch_update", {"updates": {"a": "1", "b": "2"}})
# 🔔 客户端收到多个通知：
#   - ResourceUpdated("data://a")
#   - ResourceUpdated("data://b")
#   - ResourceListChanged
# 客户端可以选择刷新所有变化的资源
```

> **给老板看的一句话总结**：后台数据一变，所有连着的 AI 客户端会自动收到通知并刷新数据——实时同步，不需要手动刷新。

---

## 十、场景 9：SSE 传输 + Streamable HTTP 选型

### 🎯 业务场景

> **故事**：CTO 问你："我们的 AI 工具服务打算上线了，有这些需求——Claude Desktop 本地要能用、Web 端也要能用、还要支持多台服务器负载均衡。你给我一个传输方式的选型方案。"
>
> **直观理解**：选传输方式就像选"沟通方式"——
> - **stdio**（面对面）：两个人坐在一起传纸条，最快最简单，但只能同一个房间里用
> - **Streamable HTTP 有状态**（电话）：可以远程通话，还能实时传情绪（语气），但电话号码得记住
> - **Streamable HTTP 无状态**（发短信/邮件）：每条消息独立，不用保持连接，适合群发和转发
> - **SSE**（对讲机）：老式方案，兼容性好，但功能不如电话
>
> **选型原则**：没有"最好的"，只有"最适合场景的"。

### Streamable HTTP（推荐 - 生产环境）

```python
# 有状态模式（支持 SSE 流式通知 + 会话管理）
mcp.run(transport="streamable-http", host="0.0.0.0", port=8000)

# 无状态模式（适合负载均衡、Serverless）
mcp.run(transport="streamable-http", host="0.0.0.0", port=8000, stateless_http=True)

# JSON 响应模式（无 SSE 流，纯 JSON 请求/响应）
mcp.run(
    transport="streamable-http",
    host="0.0.0.0",
    port=8000,
    json_response=True,
    stateless_http=True,
)
```

### SSE 传输（兼容旧客户端）

```python
mcp.run(transport="sse", host="0.0.0.0", port=8000)
```

### 选型建议

| 场景 | 推荐传输 | 原因 | 类比 |
|---|---|---|---|
| Claude Desktop / 本地工具 | stdio | 简单、安全、零配置 | 面对面传纸条 |
| Web 应用 / API 服务 | Streamable HTTP (stateless) | 可水平扩展 | 发短信 |
| 需要实时推送通知 | Streamable HTTP (stateful) | 支持 SSE 流式通知 | 打电话 |
| 需要断线重连 | Streamable HTTP + EventStore | 基于 Last-Event-ID 恢复 | 录音电话 |
| 旧客户端兼容 | SSE | 向后兼容 | 对讲机 |

> **给老板看的一句话总结**：本地用 stdio，上云用 HTTP——我们可以根据不同场景选择最合适的通信方式，就像选电话还是短信一样简单。

---

## 十一、场景 10：单元测试 —— 用 Client 直连测试服务器

### 🎯 业务场景

> **故事**：你的团队已经开发了 20 多个 MCP 工具。每次改代码都担心把之前的工具搞坏了。你需要给每个工具写自动化测试——确保改了代码后，一跑测试就知道有没有问题。
>
> **直观理解**：就像汽车工厂的质检流水线——每辆车下线前都要过一遍检测：方向盘能不能转、刹车灵不灵、灯亮不亮。如果某一项不通过，就知道哪里出了问题。
>
> MCP 的 `Client(mcp_server)` 内存直连方式天然适合写测试：不需要启动服务器、不需要网络——在 pytest 里直接调用工具、验证结果。几秒钟就跑完全部测试。
>
> **解决的痛点**：每次代码改动后，1 条命令、3 秒钟就能验证所有工具是否正常工作。

### 代码

```python
# test_my_tools.py
import pytest
from mcp.client.client import Client
from my_server import mcp  # 导入你的 MCPServer 实例

@pytest.fixture
async def client():
    async with Client(mcp, raise_exceptions=True) as c:
        yield c

@pytest.mark.anyio
async def test_add_tool(client: Client):
    result = await client.call_tool("add", {"a": 3, "b": 5})
    assert result.content[0].text == "8"

@pytest.mark.anyio
async def test_add_structured_output(client: Client):
    result = await client.call_tool("add", {"a": 3, "b": 5})
    # 如果工具返回结构化输出
    assert result.structured_content == {"result": 8}

@pytest.mark.anyio
async def test_list_tools(client: Client):
    tools = await client.list_tools()
    tool_names = [t.name for t in tools.tools]
    assert "add" in tool_names

@pytest.mark.anyio
async def test_resource(client: Client):
    result = await client.read_resource("config://settings")
    assert "theme" in result.contents[0].text

@pytest.mark.anyio
async def test_prompt(client: Client):
    result = await client.get_prompt("code_review", {"code": "print('hi')"})
    assert len(result.messages) > 0

@pytest.mark.anyio
async def test_tool_error(client: Client):
    """Test that invalid args raise an error"""
    with pytest.raises(Exception):
        await client.call_tool("add", {"a": "not_a_number", "b": 5})
```

### 运行测试

```bash
# 一条命令跑完所有测试
uv run --frozen pytest test_my_tools.py -v

# 输出示例：
# test_my_tools.py::test_add_tool PASSED           ← 加法工具：✅ 正常
# test_my_tools.py::test_add_structured_output PASSED  ← 结构化输出：✅ 正常
# test_my_tools.py::test_list_tools PASSED          ← 工具列表：✅ 正常
# test_my_tools.py::test_resource PASSED            ← 资源读取：✅ 正常
# test_my_tools.py::test_prompt PASSED              ← 提示模板：✅ 正常
# test_my_tools.py::test_tool_error PASSED          ← 错误处理：✅ 正常
# ============ 6 passed in 0.8s ============        ← 不到 1 秒，全部通过 🎉
```

> **给老板看的一句话总结**：现在每次改代码，1 秒钟就能自动检查所有 AI 工具是否正常，有问题立刻报警。

---

## 十二、场景 11：多服务器聚合 —— ClientSessionGroup

### 🎯 业务场景

> **故事**：公司不同团队各自开发了 MCP 服务器——数学团队有计算服务、文件团队有文件服务、数据团队有数据库服务。现在产品经理要做一个统一的 AI 助手，能同时使用所有团队的工具。问题是：客户端要同时连 N 个服务器，怎么管理？
>
> **直观理解**：就像一个秘书同时帮老板对接多个部门——
> - 老板说"帮我算一下预算" → 秘书知道这该找财务部
> - 老板说"帮我找一下合同" → 秘书知道这该找法务部
> - 老板不需要知道找哪个部门，秘书自动路由
>
> `ClientSessionGroup` 就是这个"超级秘书"——它同时连接多个 MCP 服务器，统一管理所有工具，自动把请求路由到正确的服务器。
>
> **解决的痛点**：多个团队的 MCP 服务器统一对外暴露，客户端一套代码管所有工具。

### 代码

```python
import anyio
from mcp.client.session_group import ClientSessionGroup
from mcp import StdioServerParameters

async def main():
    async with ClientSessionGroup() as group:
        # 连接多个服务器
        session1 = await group.connect_to_server(
            StdioServerParameters(command="python", args=["math_server.py"])
        )
        session2 = await group.connect_to_server(
            StdioServerParameters(command="python", args=["file_server.py"])
        )

        # 聚合后的工具列表（所有服务器的工具）
        print("所有可用工具：")
        for name, tool in group.tools.items():
            print(f"  {name}: {tool.description}")

        # 统一调用（自动路由到正确的服务器）
        result = await group.call_tool("add", {"a": 1, "b": 2})
        print(f"数学结果: {result.content[0].text}")

        result = await group.call_tool("read_file", {"path": "/tmp/test.txt"})
        print(f"文件内容: {result.content[0].text}")

        # 断开单个服务器
        await group.disconnect_from_server(session1)

anyio.run(main)
```

### 交互说明

统一 AI 助手的工作流程：

```
# ===== AI 助手同时连接了两个服务器 =====
所有可用工具：
  add: Add two numbers                    ← 🔢 来自 math_server（数学服务）
  multiply: Multiply two numbers          ← 🔢 来自 math_server
  read_file: Read a file                  ← 📄 来自 file_server（文件服务）
  write_file: Write a file                ← 📄 来自 file_server

# ===== 用户不需要知道工具来自哪个服务器 =====
# 用户说："帮我算 1+2"
数学结果: 3                               ← ClientSessionGroup 自动路由到 math_server ✅

# 用户说："帮我读 /tmp/test.txt"
文件内容: Hello, World!                   ← ClientSessionGroup 自动路由到 file_server ✅

# 架构图：
#
#   用户 → AI (LLM) → ClientSessionGroup ─┬→ math_server (计算)
#                                          ├→ file_server (文件)
#                                          └→ data_server (数据库)
#
# 对 AI 来说，这就是一个拥有 6 个工具的超级服务器
```

> **给老板看的一句话总结**：一个 AI 助手同时连接多个服务，自动路由请求——用户不需要知道背后有几个服务器。

---

## 十三、场景 12：Completions —— 自动补全支持

### 🎯 业务场景

> **故事**：你的 MCP 工具提供"城市天气查询"和"城市旅游指南"功能。用户在 AI 客户端输入城市名时，希望像搜索引擎一样有自动补全提示——输入"北"就弹出"北京"，输入"上"就弹出"上海"。
>
> **直观理解**：就像手机输入法的联想功能——你打一个"你"字，下面会建议"你好"、"你们"、"你的"。Completions 就是给 MCP 的资源参数和 Prompt 参数加上了"输入联想"功能，让 AI 客户端的用户体验更丝滑。
>
> **解决的痛点**：用户不需要记住精确的参数值（如完整城市名），输几个字就能选到正确的选项。

### 代码

```python
from mcp.server.mcpserver import MCPServer

mcp = MCPServer("Completion Demo")

# 定义自动补全处理器
@mcp.completion()
async def complete_city(name: str, argument_value: str) -> list[str]:
    """Provide city name completions"""
    cities = ["北京", "上海", "广州", "深圳", "杭州", "成都", "武汉"]
    if argument_value:
        return [c for c in cities if argument_value in c]
    return cities

@mcp.resource("weather://{city}")
def get_weather(city: str) -> str:
    return f"{city}: 晴天，25°C"

@mcp.prompt()
def city_guide(city: str) -> str:
    return f"请介绍 {city} 的旅游景点"
```

### 客户端交互

模拟用户在 AI 客户端输入城市名的场景：

```python
from mcp.types import ResourceTemplateReference

# ===== 场景：用户输入"北"，AI 客户端请求自动补全 =====
result = await session.complete(
    ref=ResourceTemplateReference(type="ref/resource", uri="weather://{city}"),
    argument={"name": "city", "value": "北"},
)
print(result.completion.values)  # ["北京"]
# 客户端 UI 弹出下拉：
#   ┌──────────┐
#   │ 🔍 北|    │  ← 用户输入了"北"
#   ├──────────┤
#   │ 📍 北京   │  ← 自动补全建议
#   └──────────┘

# ===== 场景：用户选择了"北京"，AI 查询天气 =====
await session.read_resource("weather://北京")
# → "北京: 晴天，25°C"

# ===== Prompt 参数也支持自动补全 =====
from mcp.types import PromptReference

result = await session.complete(
    ref=PromptReference(type="ref/prompt", name="city_guide"),
    argument={"name": "city", "value": "上"},
)
print(result.completion.values)  # ["上海"]
# 用户选择"上海"后，AI 使用 city_guide 模板生成旅游攻略
```

> **给老板看的一句话总结**：用户输入参数时有智能补全提示，体验就像搜索引擎一样丝滑。
